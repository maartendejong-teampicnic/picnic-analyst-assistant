# Knowledge files — ignore all by default; only explicitly shared files are committed
knowledge/*
!knowledge/INDEX.yaml
!knowledge/sql-snowflake.md

# Context files — ignore all by default; only shared Picnic vocab is committed
context/*
!context/picnic-business.md

# Personal files — per-user, not committed
user-config.md
TASKS.md
CONTEXT.md
tasks/
direct/
