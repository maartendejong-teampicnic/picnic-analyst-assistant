---
name: costs
description: Show a breakdown of all Claude API costs incurred via Claude Code, grouped by session and date.
allowed-tools:
  - Bash
---

# Claude API Cost Overview

Run the cost calculator script and display the output to the user:

```bash
python3 ~/Documents/Claude/analysistant/skills/costs/costs.py
```

Display the full output as-is (preformatted text). No commentary needed — the output is self-explanatory.
